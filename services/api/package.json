{
  "name": "@skyflow/api",
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "lint": "eslint src test",
    "test": "vitest run",
    "test:integration": "vitest run"
  },
  "dependencies": {
    "@skyflow/application": "0.1.0",
    "@skyflow/aws-adapters": "0.1.0",
    "@skyflow/shared": "0.1.0",
    "zod": "^3.24.2"
  }
}
